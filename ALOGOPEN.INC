Procedure OpenFiles;
begin
  Reset ( CMstFile   );
  Reset ( ContaFile  );
  Reset ( LoteFile   );
  Reset ( DeptFile   );
  Reset ( ExpeFile   );
  Reset ( SaldFile   );
  Reset ( LctoFile   );
end;